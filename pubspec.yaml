name: x_message
description: Show Toast, Message and Loading in Flutter.
version: 0.1.0
homepage: https://github.com/TaipaXu/x_message
repository: https://github.com/TaipaXu/x_message
issue_tracker: https://github.com/TaipaXu/x_message/issues

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0


flutter:
